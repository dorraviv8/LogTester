apiVersion: v1
kind: Service
metadata:
  name: {{ include "logtester-api.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "logtester-api.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000

